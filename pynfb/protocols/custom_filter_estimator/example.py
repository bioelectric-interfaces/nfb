# This is an example file with function "get_filter"
from numpy import array


def get_filter(data):
    """
    This function estimates filter for recorded data
    :param data: recorded data
    :return: _filter (numpy array or list)
    """
    _filter = array([])
    return _filter
